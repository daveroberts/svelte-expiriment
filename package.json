{
  "name": "svelte-expiriment",
  "version": "1.0.0",
  "description": "",
  "main": "output.js",
  "scripts": {
    "browser-sync": "browser-sync start --server 'dist' --files 'dist' --browser firefox",
    "build": "rollup -c",
    "build:watch": "rollup -c -w",
    "start": "npm run build:watch & npm run browser-sync"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.18.5",
    "rollup": "^0.38.0",
    "rollup-plugin-buble": "^0.15.0",
    "rollup-plugin-svelte": "^1.6.0",
    "rollup-plugin-uglify": "^1.0.1",
    "rollup-watch": "^2.5.0"
  }
}
